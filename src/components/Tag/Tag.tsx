import React from "react";
import TagProps from "./Tag.types";

export const Tag = ({ text, classes, attributes, html }: TagProps) => {
  return <strong class="govuk-tag">alpha</strong>;
};

export default Tag;
